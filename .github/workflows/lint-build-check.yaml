name: Lint and Build Check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint-and-build:
    name: Lint and Build Client and Server
    runs-on: ubuntu-latest

    strategy:
      matrix:
        app: [client, server]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'
          cache: 'npm'

      # Install dependencies
      - name: Install dependencies
        working-directory: ${{ matrix.app }}
        run: npm install

      # Run linter
      - name: Run linter
        working-directory: ${{ matrix.app }}
        run: npm run lint

      # Build the app
      - name: Build the app
        working-directory: ${{ matrix.app }}
        run: npm run build
